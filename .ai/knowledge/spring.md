# Spring (agent knowledge)

## Purpose
Spring Boot track. Target Spring Boot 4.1.x / Framework 7.x (no backend in repo yet).

## Core concepts (track scope)
DI & bean lifecycle · @Transactional semantics & self-invocation · AOP proxies ·
Web layer (@RestController, async) · Security (filter chains, JWT/OAuth2) ·
Data (JPA/Hibernate, transactions, N+1) · Actuator & observability · Test slices.

## Project conventions
- Canonical explanations (Transactional outbox, Redlock…) live in seedData.ts — reference.
- Backend build will be Maven (target). No pom.xml exists — do not create one without
  an approved phase.

## Current relevance
/learning/spring is a placeholder route. Backend not started.

## Related
Skills: backend-spring, architecture-lab · Knowledge: java.md, microservices.md
